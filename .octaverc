% produces pretty good figures with "print -dpdf foo.pdf"
set(0,'defaultaxesfontsize',12.0)
set(0,'defaultlinelinewidth',3.0)

%graphics_toolkit gnuplot    % helps avoid some stupid print behavior

pkg load all   % e.g. odepkg

% real Matlab compatibility: make all Octave stupidity (extensions) into errors
% (never mind .. things like 'help' and 'ls' throw errors)
%warning('on','Octave:language-extension')
%warning('error')

